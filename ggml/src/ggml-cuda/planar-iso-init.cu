/*
 * planar-iso-init.cu — stub file.
 * Rotation params are now computed on-the-fly via inline LCG in planar-iso-dequant.cuh.
 * No host-side buffers needed. This TU is kept only to satisfy CMake's GLOB pattern.
 */
